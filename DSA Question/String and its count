s = 'Banana'
sb = 'nn'
// whether there is a substring and what are the counts
function subString(s, sb) {
  let count = 0
  for (let i = 0; i < s.length; i++) {
    if ( s.substring(i, sb.length + i) === sb){
      count += 1;
    }
  }
  return count;
}

console.log(subString(s,sb));